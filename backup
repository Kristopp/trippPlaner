//   const createNewTab = () => {
    console.log(fromDB)
    //Create new object and push into array
    let newObject = new Object({
      id: 0,
      title: "Where?",
      picture: "",
      date: "when we go?",
    });
    setId(id + 1);
    newObject.id = id;
    tabsArray.push(newObject);
  };